# Моки для API

## Быстрый старт

### 1. Включить моки

В файле `NetworkModule.kt` установите:

```kotlin
const val USE_MOCK_API = true
```

### 2. Запустите приложение

Теперь все API запросы будут возвращать тестовые данные без подключения к backend.

## Что включено

### 📝 Тестовые данные

- **5 сотрудников** с полными профилями
- **4 новости** разных категорий
- **3 дня рождения**
- **3 достижения**
- **4 задачи** в разных статусах
- **3 отпуска** (запланированные и одобренные)
- **4 учебных курса** с прогрессом
- **5 рабочих мест** для бронирования
- **8 товаров мерча** в разных категориях
- **4 карточки избранного**

### ⚙️ Возможности

✅ **Реалистичная задержка** - 500ms симуляция сетевых запросов  
✅ **Динамическое состояние** - добавление/удаление избранного, покупка мерча  
✅ **Валидация** - проверка данных при авторизации и покупках  
✅ **Поиск** - фильтрация сотрудников по имени, должности, отделу

## Примеры использования

### Авторизация

```kotlin
// Любые непустые значения пройдут проверку
apiService.login("ivan.ivanov", "password")
// Вернет токен и данные пользователя
```

### Поиск сотрудников

```kotlin
// Поиск по имени, фамилии, должности или отделу
apiService.searchEmployees("дизайн")
// Вернет: Елена Смирнова (UI/UX Designer)
```

### Покупка мерча

```kotlin
// У пользователя 500 баллов
apiService.purchaseMerchItem("merch_1") // Футболка за 100 баллов
// Успех! Теперь 400 баллов

apiService.purchaseMerchItem("merch_3") // Рюкзак за 300 баллов  
// Успех! Теперь 100 баллов

apiService.purchaseMerchItem("merch_2") // Худи за 250 баллов
// Ошибка: недостаточно баллов
```

### Работа с избранным

```kotlin
// Получить список
val favorites = apiService.getFavorites()
// [Jira, Confluence, GitLab, Корпоративный портал]

// Добавить новую карточку
apiService.addFavorite("Slack", "https://slack.tutu.ru")

// Удалить
apiService.deleteFavorite("fav_1")
```

## Настройка

### Изменить задержку сети

```kotlin
// В MockApiService.kt
private val networkDelay = 100L  // Было: 500L
```

### Изменить тестовые данные

Отредактируйте методы в `MockApiService.kt`:

```kotlin
private fun getMockUser(): UserDto {
    return UserDto(
        id = "user_1",
        firstName = "Ваше",
        lastName = "Имя",
        position = "Ваша Должность",
        // ... другие поля
    )
}
```

### Добавить больше данных

```kotlin
private fun getMockNewsList(): List<NewsDto> {
    return listOf(
        // Добавьте больше новостей
        NewsDto(
            id = "news_5",
            title = "Новая новость",
            content = "Описание",
            // ...
        )
    )
}
```

## Переключение на реальный API

Когда backend будет готов:

```kotlin
// В NetworkModule.kt
const val USE_MOCK_API = false

// И укажите правильный URL
install(DefaultRequest) {
    url("https://api.tutu.ru/employee")
}
```

## Преимущества

🚀 **Быстрая разработка** - не нужно ждать backend  
💻 **Работа офлайн** - разработка без интернета  
🎯 **Предсказуемые данные** - всегда одинаковый набор для тестирования  
🐛 **Легкая отладка** - контролируемые сценарии  
📱 **Демонстрация** - можно показывать без backend

## Структура

```
composeApp/src/commonMain/kotlin/ru/tutu/tutuemployee/
├── data/remote/api/
│   ├── ApiService.kt          # Интерфейс
│   ├── ApiServiceImpl.kt      # Реальная реализация
│   └── MockApiService.kt      # Мок реализация
└── di/
    └── NetworkModule.kt        # Конфигурация (переключатель USE_MOCK_API)
```

## Подробная документация

Полное руководство см. в [MOCK_API_GUIDE.md](./MOCK_API_GUIDE.md)
