# Tutu Employee - Приложение для сотрудников

Мультиплатформенное приложение для сотрудников компании, созданное с использованием Compose
Multiplatform.

## 🎯 Основные возможности

### 1. **Главная страница** 🏠

- Новости компании с категориями
- Дни рождения коллег с карточками
- Поиск по сотрудникам и отделам в реальном времени
- Быстрый доступ к информации о коллегах

### 2. **Профиль пользователя** 👤

- Личная информация (имя, фамилия, должность, отдел, юр. лицо)
- Достижения (ачивки) с визуализацией
- Текущие задачи с статусами (TODO, IN_PROGRESS, DONE)
- Запланированные отпуска с календарем
- Доступные дни отпуска
- Доступные курсы (iSpring) с прогрессом обучения
- Баланс внутренних баллов

### 3. **Офис** 🏢

- Бронирование мест в опенспейсе
- Визуальная карта рабочих мест (4x4 grid)
- Индикация занятых и свободных мест
- Интеграция с WebView для бронирования
- Новости офиса
- Выбор даты для бронирования

### 4. **Магазин мерча** 🛒

- Витрина товаров компании
- Фильтрация по категориям:
    - Одежда
    - Аксессуары
    - Канцелярия
    - Электроника
- Покупка за внутренние баллы
- Отображение баланса баллов
- Индикация наличия товаров

### 5. **Избранное** ⭐

- Создание персональных карточек с ссылками
- Название и URL для каждой карточки
- Открытие ссылок через WebView
- Удаление карточек из избранного

### 6. **Авторизация** 🔐

- OAuth авторизация
- Ввод логина и пароля
- Защищенный доступ к приложению
- Сохранение токена авторизации

## 🏗️ Архитектура

### Технологический стек

- **Compose Multiplatform** - UI фреймворк
- **Kotlin** 2.2.21
- **Ktor** 3.1.1 - HTTP клиент для API
- **Kotlinx Serialization** - сериализация JSON
- **Kotlinx Coroutines** - асинхронность
- **Kotlinx DateTime** - работа с датами
- **Navigation Compose** - навигация
- **Lifecycle ViewModel** - управление состоянием

### Поддерживаемые платформы

- ✅ **Android** (API 24+)
- ✅ **Web** (JS/Wasm)
- ⚠️ **iOS** (в разработке)

### Структура проекта

```
composeApp/
├── commonMain/
│   └── kotlin/
│       └── ru/tutu/tutuemployee/
│           ├── data/
│           │   ├── model/          # Data models
│           │   └── network/        # API client и service
│           ├── navigation/         # Navigation setup
│           ├── presentation/       # UI слой
│           │   ├── auth/          # Авторизация
│           │   ├── home/          # Главная страница
│           │   ├── profile/       # Профиль
│           │   ├── office/        # Офис
│           │   ├── merch/         # Магазин мерча
│           │   ├── favorites/     # Избранное
│           │   ├── webview/       # WebView
│           │   └── components/    # Общие компоненты
│           └── App.kt             # Точка входа
├── androidMain/                    # Android специфичный код
├── jsMain/                         # Web специфичный код
└── iosMain/                        # iOS специфичный код
```

## 🔌 API Integration

Приложение взаимодействует с backend через REST API:

### Endpoints

#### Авторизация

- `POST /auth/login` - вход в систему

#### Пользователь

- `GET /user/me` - текущий пользователь
- `GET /search/employees?q=query` - поиск сотрудников

#### Главная

- `GET /news` - новости компании
- `GET /birthdays` - дни рождения

#### Профиль

- `GET /profile/achievements` - достижения
- `GET /profile/tasks` - задачи
- `GET /profile/vacations` - отпуска
- `GET /profile/courses` - курсы

#### Офис

- `GET /office/workspaces?date=YYYY-MM-DD` - рабочие места
- `GET /office/news` - новости офиса

#### Магазин

- `GET /merch` - список товаров
- `POST /merch/{id}/purchase` - покупка товара

#### Избранное

- `GET /favorites` - список избранного
- `POST /favorites` - добавить в избранное
- `DELETE /favorites/{id}` - удалить из избранного

### Настройка API

Измените URL в `ApiClient.kt`:

```kotlin
private const val BASE_URL = "https://api.tutu.ru/employee"
```

## 🚀 Запуск проекта

### Android

```bash
./gradlew :composeApp:assembleDebug
```

### Web (JS)

```bash
./gradlew :composeApp:jsBrowserDevelopmentRun
```

### Web (Wasm)

```bash
./gradlew :composeApp:wasmJsBrowserDevelopmentRun
```

### Desktop

```bash
./gradlew :composeApp:run
```

## 🎨 UI/UX особенности

- **Material 3 Design** - современный дизайн
- **Emoji иконки** - кроссплатформенная поддержка
- **Адаптивная верстка** - работает на всех размерах экранов
- **Pull-to-Refresh** - обновление данных
- **Skeleton загрузки** - индикаторы загрузки
- **Обработка ошибок** - информативные сообщения

## 🔐 Безопасность

- Bearer Token авторизация
- Хранение токена в памяти
- Защищенные API запросы
- Валидация на клиенте

## 📱 Особенности реализации

### WebView

WebView реализован как заглушка в common коде. Для каждой платформы требуется специфичная
реализация:

- **Android**: WebView
- **iOS**: WKWebView
- **Web**: iframe

### Navigation

Используется Compose Navigation для мультиплатформы. Текущая версия использует упрощенную навигацию
без типобезопасности из-за особенностей сборки.

### Иконки

Вместо Material Icons используются emoji для кроссплатформенной совместимости.

## 🛠️ Разработка

### Добавление нового экрана

1. Создайте Screen в `navigation/Screen.kt`:

```kotlin
@Serializable
data object NewScreen : Screen
```

2. Создайте ViewModel:

```kotlin
class NewViewModel : ViewModel() {
    // ...
}
```

3. Создайте Composable экран:

```kotlin
@Composable
fun NewScreen(navController: Any) {
    // ...
}
```

4. Добавьте маршрут в `NavigationHost.kt`

### Добавление API endpoint

1. Добавьте метод в `ApiService.kt`:

```kotlin
suspend fun getNewData(): Result<Data> {
    return try {
        val response = client.get("/new-endpoint")
        Result.success(response.body<Data>())
    } catch (e: Exception) {
        Result.failure(e)
    }
}
```

2. Используйте в ViewModel

## 📝 TODO

- [ ] Реализация полноценной навигации с NavHostController
- [ ] Добавление Material Icons для платформ
- [ ] WebView реализация для каждой платформы
- [ ] Offline режим с кешированием
- [ ] Push уведомления
- [ ] Биометрическая авторизация
- [ ] Темная тема
- [ ] Локализация (EN/RU)
- [ ] Unit тесты
- [ ] UI тесты

## 🤝 Contributing

1. Fork проект
2. Создайте feature branch
3. Commit изменения
4. Push в branch
5. Создайте Pull Request

## 📄 License

Проект создан для внутреннего использования в компании Tutu.ru

---

**Версия**: 1.0.0  
**Дата**: Декабрь 2024  
**Платформы**: Android, Web (JS/Wasm)
