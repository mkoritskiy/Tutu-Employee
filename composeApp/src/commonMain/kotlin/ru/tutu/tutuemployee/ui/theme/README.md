# Material3 Theme для Tutu Employee

## Обзор

Приложение Tutu Employee использует стильную Material3 тему с корпоративными цветами и современными
UI компонентами.

## Структура темы

### Файлы темы

- **Color.kt** - Определение цветовой палитры приложения
- **Theme.kt** - Основная тема приложения с светлым и темным режимами
- **Type.kt** - Типографика (шрифты и стили текста)
- **Shape.kt** - Формы элементов (скругления углов)

## Цветовая палитра

### Основные цвета

- **Primary** - Синий (`#1976D2`) - основной корпоративный цвет
- **Secondary** - Оранжевый (`#FF6F00`) - акцентный цвет
- **Tertiary** - Бирюзовый (`#00897B`) - дополнительный акцент

### Светлая тема

- Фон: `#FAFAFA`
- Поверхности: `#FFFFFF`
- Текст: `#1C1B1F`

### Темная тема

- Фон: `#1C1B1F`
- Поверхности: `#1C1B1F`
- Текст: `#E6E1E5`

## Типографика

Приложение использует системные шрифты с различными стилями:

### Display (крупные заголовки)

- Display Large: 57sp
- Display Medium: 45sp
- Display Small: 36sp

### Headline (заголовки разделов)

- Headline Large: 32sp, Bold
- Headline Medium: 28sp, Bold
- Headline Small: 24sp, SemiBold

### Title (заголовки карточек)

- Title Large: 22sp, SemiBold
- Title Medium: 16sp, SemiBold
- Title Small: 14sp, Medium

### Body (основной текст)

- Body Large: 16sp
- Body Medium: 14sp
- Body Small: 12sp

### Label (кнопки и метки)

- Label Large: 14sp, Medium
- Label Medium: 12sp, Medium
- Label Small: 11sp, Medium

## Формы (Shapes)

- **Extra Small**: 4dp - малые элементы
- **Small**: 8dp - чипы, кнопки
- **Medium**: 12dp - карточки
- **Large**: 16dp - диалоги
- **Extra Large**: 28dp - модальные окна

## Использование

### В коде

```kotlin
import ru.tutu.tutuemployee.ui.theme.TutuEmployeeTheme

@Composable
fun MyScreen() {
    TutuEmployeeTheme {
        // Ваш UI код здесь
        Card(
            colors = CardDefaults.cardColors(
                containerColor = MaterialTheme.colorScheme.primaryContainer
            ),
            elevation = CardDefaults.cardElevation(defaultElevation = 2.dp)
        ) {
            Text(
                text = "Привет!",
                style = MaterialTheme.typography.titleLarge,
                color = MaterialTheme.colorScheme.onPrimaryContainer
            )
        }
    }
}
```

### Доступ к цветам

```kotlin
// Основной цвет
MaterialTheme.colorScheme.primary

// Фон карточки
MaterialTheme.colorScheme.primaryContainer

// Текст на карточке
MaterialTheme.colorScheme.onPrimaryContainer
```

### Типографика

```kotlin
Text(
    text = "Заголовок",
    style = MaterialTheme.typography.headlineSmall
)

Text(
    text = "Обычный текст",
    style = MaterialTheme.typography.bodyMedium
)
```

### Формы

```kotlin
Card(
    shape = MaterialTheme.shapes.medium // 12dp скругление
) {
    // Содержимое
}

Button(
    shape = MaterialTheme.shapes.small // 8dp скругление
) {
    Text("Кнопка")
}
```

## Стильные компоненты

В `presentation/components/StyledComponents.kt` доступны готовые компоненты:

### SectionHeader

Заголовок секции с иконкой:

```kotlin
SectionHeader(
    title = "Новости",
    icon = "📰"
)
```

### IconCard

Карточка с иконкой:

```kotlin
IconCard(
    icon = "🎂",
    title = "День рождения",
    subtitle = "Иван Иванов"
)
```

### InitialsAvatar

Аватар с инициалами:

```kotlin
InitialsAvatar(
    initials = "ИИ",
    size = 48
)
```

### StatCard

Карточка со статистикой:

```kotlin
StatCard(
    icon = "⭐",
    value = "150",
    label = "бонусов"
)
```

### EmptyState

Пустое состояние:

```kotlin
EmptyState(
    icon = "📭",
    title = "Пусто",
    subtitle = "Добавьте первый элемент"
)
```

## Рекомендации по дизайну

### Elevation (поднятие)

- Карточки: 2dp
- Кнопки: 4dp
- Диалоги: 8dp

### Отступы

- Малые: 8dp
- Средние: 16dp
- Большие: 24dp
- Экстра большие: 32dp

### Иконки

Используйте эмоджи для иконок:

- 🏠 - Главная
- 👤 - Профиль
- 🏢 - Офис
- 🛒 - Магазин
- ⭐ - Избранное

## Темный режим

Тема автоматически адаптируется к системным настройкам:

```kotlin
TutuEmployeeTheme(
    darkTheme = isSystemInDarkTheme()
) {
    // UI
}
```

Или можно задать явно:

```kotlin
TutuEmployeeTheme(
    darkTheme = true // Всегда темная тема
) {
    // UI
}
```

## Accessibility (Доступность)

- Все цвета соответствуют требованиям контрастности WCAG AA
- Минимальный размер текста: 12sp
- Минимальный размер кликабельных элементов: 48dp
- Поддержка увеличения шрифта в системе

## Совместимость

Тема совместима со всеми платформами:

- ✅ Android
- ✅ iOS
- ✅ Web (JS/WASM)
- ✅ Desktop

## Обновления

При необходимости изменения цветовой схемы редактируйте файл `Color.kt`:

1. Измените значения цветов
2. Проверьте контрастность
3. Протестируйте в светлом и темном режимах
4. Обновите эту документацию
